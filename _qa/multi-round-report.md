# 多轮回归报告（2026-09-04）

## 结论

本地实验版通过 26 个测试命令。故障、并发、持久化、迁移和 Prolog 边界组连续执行 3 轮，结果稳定；浏览器测试没有调用线上模型，也没有写生产数据。

此外，真实服务器联调通过：Story Session 经 SSH loopback 隧道调用 SWI-Prolog 10.1.13，先验证前置条件拒绝，再完成 10 个确定性动作，最终 `signal=6`、`trust=3`、章节完成。该真实服务器测试不包含在默认 26 个离线命令中，需显式运行 `npm run test:prolog-live`。

## 覆盖

- 核心合同：Story Session 类型、协议、角色首次登场、雾港完整领域路线、权威影子、服务端回合、Prolog 客户端与身份边界。
- 3 轮故障回归：响应丢失、动作重放、版本竞争、事务回滚、进程重启、磁盘恢复、会话目录、旧存档迁移、规则服务不一致零写入。
- AlterU 用户系统：A/B 用户资料、平台存档 bootstrap、同用户刷新续玩、无本地缓存的新设备恢复、稳定 session、跨用户 404、用户级本地缓存隔离。
- 浏览器端到端：会话重开与切换、未知响应恢复、完整确定性通关、安雅首次登场、中文 390×844、英文 320×568、44px 触控目标、无横向溢出和外部访客状态。
- 真实 Prolog：Story Session 状态注入、前置拒绝、完整章节、材料原子消耗；原始服务并发 30 轮出现 19 次双成功，扩展 50 轮为 0 次。

## 可复跑命令

```bash
npm run test:multi-round

# 需要已建立 SSH loopback 隧道
npm run test:prolog-live
```

## 仍未覆盖

- Prolog 常驻进程托管；
- Worker 到受保护 HTTPS/私网规则网关的线上调用与服务端 token 验证；
- 生产 Worker secret 与线上数据库；
- AlterU 服务端签名身份。当前 `alteru-user-id-v1-experimental` 完成功能绑定，但用户 ID 仍由前端传入。
